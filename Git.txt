Complete git flow stageing sb btao
git init 
git add filename 
git commit -m  
git --version
git status  
git log 
git log --oneline 
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
git config --list 
git config --global core.editor "code --wait" 
gitignore 
gitkeep 
Complete git flow
Branches in Git 
head in git  
git branch  
git branch NEW_BRANCH_NAME 
git switch BRANCH_NAME 
git switch -c NEW_BRANCH_NAME
git chechout BRANCH_NAME 
git chechout -b NEW_BRANCH_NAME
Merging branches 
Fast forword merge
merge conflict  
manaual conflict reolsve
Current branch ka code rakhna (OURS) 
git checkout --ours second.txt
git add second.txt
git commit -m "kept current branch version"
Incoming branch ka code rakhna (THEIRS)
git checkout --theirs second.txt
git add second.txt
git commit -m "kept incoming branch version"
Git Tags
git tag <tag-name>
git tag -a <tag-name> -m "Release 1.0"
git tag
git tag <tag-name> <commit-hash>
git push origin <tag-name>
git tag -d <tag-name>
git push origin :<tag-name>
